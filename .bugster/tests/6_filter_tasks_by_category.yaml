name: Filter tasks by category
page: /
page_path: app/page.tsx
task: Filtering tasks by category
steps:
- Navigate to the home page
- Locate the category filter dropdown
- Select a specific category
- Verify only tasks from the selected category are displayed
expected_result: Only tasks from the selected category should be displayed
