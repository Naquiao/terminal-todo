name: Task Filtering
page: /
page_path: app/page.tsx
task: Filtering tasks by category or status
steps:
- Navigate to the homepage
- Click on the filter dropdown
- Select a specific filter option (e.g., category or status)
- Verify only tasks matching the selected criteria are displayed
expected_result: Only tasks matching the filter criteria should be displayed
