name: Filter tasks by category
page: /
page_path: app/page.tsx
task: Filtering the task list by a specific category
steps:
- Navigate to the home page
- Click on the category filter dropdown
- Select a specific category
- Verify that only tasks with the selected category are displayed
expected_result: Only tasks matching the selected category are displayed
